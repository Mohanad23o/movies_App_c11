import 'package:flutter/material.dart';

class CategoryBrowseTab extends StatelessWidget {
  const CategoryBrowseTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
    );
  }
}
